machine:
  environment:
    TOXENV: py27
dependencies:
  pre:
    - gem install fpm
    - gem install package_cloud
    - mkdir artifacts
    - pip install virtualenv-tools
general:
  artifacts:
    - artifacts
test:
  override:
    - echo "Testing disabled"
  post:
    - ./build-deb.sh:
      pwd: fpm

